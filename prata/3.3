#include <iostream>
using namespace std;
int main()
{
   // std::cout << "Hello World!\n";
    long double a, b, c;

    a = 40.0;
    b = 40.0; 
    c = 40.0;
    long double af = (long double)a;
    long double wynik = 0.0;
    long double bf = (long double)b;
    
    b = b / 60;
    
    long double cf = (long double)c;
    c = c / 3600;

    wynik = a + b + c;
    cout << wynik;
        
}
