#include <iostream>
#include <string>
#include <vector>
#include <array>
using namespace std;
int main()
{
    double srednia = 0.0;
int a, b;
while((cin>>a >>b) && (a != 0) && (b != 0 ))
    {
        srednia = 2.0 * a * b / (a + b);
        cout<<srednia;
    }
cout<<"koniec";
}
