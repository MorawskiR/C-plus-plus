#include <iostream>
#include <string>
#include <vector>
#include <array>
using namespace std;
int replace(char * str, char c1, char c2);

int main()
{
    char znak1 = '1';
    char znak2 = '2';
   char znaki[20] = "1111111111111";
   cout<<znaki;
   replace(znaki, znak1,znak2);
   cout<<endl;
    cout<<znaki;
}
int replace(char * str, char c1, char c2)
{
    while(*str)
    {
        if(*str == c1)
        {
            *str = c2;
        }
        str++;
    }
}
