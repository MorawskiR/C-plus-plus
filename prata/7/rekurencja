

#include <iostream>
#include <string>
#include <vector>
#include <array>
using namespace std;

void countdown(int n);
int main() {
    countdown(4);
}
void countdown(int n){
    cout<<"odliczanie "<<n<<endl;
    if(n > 0)
    {
        countdown(n-1);
    }
    cout<<n<<"bac";
}
